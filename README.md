# ArchVisionAI

**ArchVisionAI** – інтелектуальна вебсистема для аналізу та класифікації архітектурних стилів за зображенням будівлі.

Проєкт поєднує моделі комп’ютерного зору, генеративний ШІ для пояснення результату, інтерактивну карту, історію аналізів користувача та вебінтерфейс для зручної роботи із завантаженими зображеннями.

## Демо

Розгорнута версія вебзастосунку доступна за посиланням:

http://archvisionai.swedencentral.cloudapp.azure.com

## Призначення проєкту

Мета ArchVisionAI – не лише визначити архітектурний стиль будівлі на зображенні, а й подати результат у зрозумілому для користувача вигляді.

Після завантаження зображення система:

- перевіряє, чи містить зображення архітектурний об’єкт;
- якщо зображення не є архітектурним, показує користувачу відповідне повідомлення;
- визначає архітектурний стиль будівлі;
- показує рівень впевненості моделі;
- відображає кілька найімовірніших варіантів класифікації;
- формує розширений текстовий опис за допомогою Gemini API;
- показує географічний контекст визначеного стилю на інтерактивній карті;
- зберігає історію аналізів для авторизованих користувачів;
- формує статистику та персональні архітектурні вподобання користувача.

Такий підхід робить застосунок корисним як навчальний, демонстраційний і довідковий інструмент для роботи з архітектурними зображеннями, культурною спадщиною, туристичними матеріалами та задачами комп’ютерного зору.

## Основні можливості

### Класифікація архітектурного стилю

Користувач може завантажити зображення будівлі через вебінтерфейс. Після цього система виконує попередню обробку зображення, передає його до моделі комп’ютерного зору та повертає прогнозований архітектурний стиль.

Класифікація виконується за **25 архітектурними стилями**.

### Попередня перевірка архітектурного зображення

Перед запуском основної моделі класифікації система виконує попередню перевірку того, чи містить завантажене зображення архітектурний об’єкт.

Для цього використовується окрема бінарна модель **EfficientNet-B0**, навчена на двох класах:

- `architecture` – зображення будівель, фасадів та архітектурних споруд;
- `not_architecture` – зображення об’єктів, які не належать до архітектури.

Якщо зображення визначається як `not_architecture`, система не запускає класифікацію архітектурного стилю, не зберігає такий запит в історію аналізів і показує користувачу повідомлення з проханням завантажити фото будівлі або архітектурної споруди.

Така перевірка потрібна через те, що основні моделі класифікації стилів навчені лише на архітектурних класах і без попередньої перевірки змушені відносити будь-яке зображення до одного з 25 стилів.

### Підтримка кількох режимів аналізу

У застосунку доступні такі режими:

- **EfficientNet-B0** – компактна та швидка згорткова нейронна мережа;
- **ResNet-50** – глибша CNN-архітектура із залишковими зв’язками;
- **Ensemble** – комбінування результатів EfficientNet-B0 і ResNet-50;
- **TTA** – test-time augmentation, тобто аугментація зображення на етапі аналізу для підвищення стабільності результату.

### Розширений опис Gemini AI

Окрім числового результату класифікації, система може сформувати пояснення за допомогою Gemini API.

Розширений опис може містити:

- визначений архітектурний стиль;
- можливу назву будівлі;
- місцезнаходження;
- архітектора;
- рік або період будівництва;
- характерні архітектурні елементи;
- історичну цінність;
- сучасне використання будівлі.

### Інтерактивна карта

Для кожного архітектурного стилю в проєкті підготовлено географічні метадані. Вони використовуються для відображення регіонів поширення стилю та прикладів відомих будівель.

Карта реалізована за допомогою **Leaflet** і підтримує:

- маркери відомих будівель;
- географічні області поширення стилів;
- інформаційні вікна з коротким описом;
- візуальне доповнення результату класифікації.

### Автентифікація користувача

Незареєстрований користувач може виконувати базовий аналіз зображення. Після реєстрації та входу в систему відкриваються додаткові можливості:

- збереження історії аналізів;
- перегляд попередніх результатів;
- збереження мініатюр зображень;
- статистика за кількістю аналізів;
- найчастіше визначені стилі;
- персональні архітектурні вподобання.

### Збереження даних

Для збереження даних використовується **SQLite**. У базі даних зберігаються:

- користувачі;
- хеші паролів і salt-значення;
- історія аналізів;
- мініатюри зображень;
- визначені архітектурні стилі;
- значення впевненості моделі;
- результати Gemini-аналізу;
- архітектурні вподобання користувача.

## Датасет архітектурних стилів

У проєкті використано публічний датасет **Architectural styles** з платформи Kaggle:

https://www.kaggle.com/datasets/dumitrux/architectural-styles-dataset

За описом датасету, він містить **10113 зображень** і охоплює **25 архітектурних стилів**. Набір даних поєднує зображення з Google Images та матеріали попереднього академічного датасету для задачі класифікації архітектурних стилів.

У репозиторії мапінг класів зберігається у файлі:

```text
data/class_mapping.json
```

Українські переклади назв архітектурних стилів зберігаються у файлі:

```text
data/architectural_styles_ukrainian.json
```

Географічні метадані для карти зберігаються у файлах:

```text
data/architectural_styles_geography.json
data/architectural_geography_ukrainian.json
```

### Датасет для бінарної перевірки зображення

Для попередньої перевірки того, чи є зображення архітектурним, використовується окрема бінарна модель.

Позитивний клас `architecture` формується із зображень архітектурних об’єктів з основного датасету **Architectural styles**.

Негативний клас `not_architecture` формується із зображень датасету **Caltech-101**:

```text
https://www.kaggle.com/datasets/imbikramsaha/caltech-101
```

Caltech-101 містить зображення багатьох різних категорій об’єктів, тому підходить для формування класу `not_architecture`. Під час підготовки класу не архітектурних об'єктів слід уникати зображень, на яких явно присутні будівлі, фасади, архітектурні споруди, пам’ятки або вулиці з великою кількістю архітектурних об’єктів.

Очікувана структура датасету для навчання бінарного фільтра:

```text
dataset/
├── train/
│   ├── architecture/
│   └── not_architecture/
└── val/
    ├── architecture/
    └── not_architecture/
```

### Підтримувані архітектурні стилі

Система працює з такими класами:

- Achaemenid architecture
- American Foursquare architecture
- American craftsman style
- Ancient Egyptian architecture
- Art Deco architecture
- Art Nouveau architecture
- Baroque architecture
- Bauhaus architecture
- Beaux-Arts architecture
- Byzantine architecture
- Chicago school architecture
- Colonial architecture
- Deconstructivism
- Edwardian architecture
- Georgian architecture
- Gothic architecture
- Greek Revival architecture
- International style
- Neo-Byzantine Revival architecture
- Novelty architecture
- Palladian architecture
- Postmodern architecture
- Queen Anne architecture
- Romanesque architecture
- Tudor Revival architecture

## Використані технології

### Backend

- Python
- Flask
- Flask-CORS
- Flask-Session
- Gunicorn
- SQLite
- python-dotenv

### Machine Learning та обробка зображень

- PyTorch
- Torchvision
- timm
- NumPy
- Pillow
- Matplotlib

### AI-інтеграція

- Google Gemini API
- google-genai

### Frontend

- HTML
- CSS
- JavaScript
- Fetch API
- Leaflet

### Розгортання

- Docker
- Gunicorn
- Microsoft Azure

## Структура проєкту

```text
ArchVisionAI/
├── app.py                         # Точка запуску Flask-застосунку
├── routes.py                      # Web/API маршрути
├── database.py                    # Логіка роботи з SQLite
├── ml_engine.py                   # Аналіз зображень, ML-інференс, Gemini та геодані
├── model_zoo.py                   # Конфігурація моделей, трансформації та checkpoint loading
├── train_arch_or_not.py           # Навчання бінарного фільтра architecture / not_architecture
├── requirements.txt               # Python-залежності
├── Dockerfile                     # Docker-конфігурація
├── .env.example                   # Приклад змінних середовища
├── data/
│   ├── class_mapping.json
│   ├── architectural_styles_ukrainian.json
│   ├── architectural_styles_geography.json
│   └── architectural_geography_ukrainian.json
├── services/
│   ├── analysis_service.py        # Сервісний шар між routes і ML-аналізатором
│   └── architecture_filter.py     # Бінарний фільтр architecture / not_architecture
├── static/
│   ├── css/                       # Стилі інтерфейсу
│   └── js/                        # Клієнтська логіка
├── templates/
│   └── index.html                 # Основна HTML-сторінка
├── test_results/                  # Результати тестування моделей
└── training_runs/                 # Результати навчання моделей
```

## Архітектура застосунку

Проєкт побудовано за багатошаровою архітектурою:

1. **Клієнтський рівень**  
   Користувач відкриває вебінтерфейс, вибирає модель, завантажує зображення та переглядає результат.

2. **Серверний рівень**  
   Flask приймає HTTP-запити, перевіряє дані, керує сесіями та повертає JSON-відповіді.

3. **Сервісний рівень**  
   `AnalysisService` готує зображення, створює мініатюру, визначає режим аналізу та координує роботу бінарного фільтра і ML-аналізатора.

4. **Рівень попередньої перевірки зображення**  
   `ArchitectureFilter` виконує бінарну класифікацію `architecture / not_architecture`. Якщо зображення не містить архітектурного об’єкта, система повертає користувачу повідомлення і не запускає основну класифікацію стилю.

5. **Рівень машинного навчання**  
   `ArchVisionAnalyzer` завантажує моделі, виконує попередню обробку архітектурного зображення, запускає інференс, застосовує softmax і формує прогноз стилю.

6. **Рівень Gemini AI**  
   Gemini API формує розширений україномовний опис архітектурного об’єкта.

7. **Рівень бази даних**  
   SQLite зберігає користувачів, історію аналізів, статистику та архітектурні вподобання.

8. **Рівень геопросторової візуалізації**  
   Leaflet відображає регіони поширення стилю та приклади відомих будівель на карті.

## Режими моделей

### Architecture / Not Architecture Filter

Перед основною класифікацією стилю використовується окрема модель **EfficientNet-B0** для перевірки типу зображення.

Модель має два вихідні класи:

- `architecture`;
- `not_architecture`.

Її checkpoint очікується у файлі:

```text
checkpoints/best_model_arch_or_not.pth
```

Якщо фільтр визначає, що зображення не є архітектурним, система повертає повідомлення користувачу й не запускає основну модель класифікації архітектурного стилю.

### EfficientNet-B0

EfficientNet-B0 використовується як компактна й швидка CNN-модель для базової класифікації архітектурних стилів. Вона має вхідний розмір зображення 224×224 пікселі та 25 вихідних класів.

### ResNet-50

ResNet-50 є глибшою згортковою нейронною мережею із залишковими зв’язками. Вона використовується як стабільна модель для класифікації зображень архітектурних фасадів.

### Ensemble

У режимі Ensemble система усереднює ймовірності, отримані від EfficientNet-B0 і ResNet-50. Це зменшує вплив помилок окремої моделі та робить підсумковий прогноз стабільнішим.

### Test-Time Augmentation

Якщо увімкнено TTA, система створює п’ять варіантів одного зображення:

- оригінальне зображення;
- дзеркальне відображення;
- зображення з підвищеною яскравістю;
- зображення з підвищеним контрастом;
- зображення з невеликим поворотом.

Після цього модель аналізує всі варіанти, а фінальні ймовірності усереднюються.

## API

### Автентифікація

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/status
```

### Аналіз зображень

```text
POST /api/analyze
POST /api/analyze/gemini
```

`/api/analyze` спочатку виконує бінарну перевірку `architecture / not_architecture`. Якщо зображення є архітектурним, маршрут запускає класифікацію архітектурного стилю. Якщо зображення не є архітектурним, система повертає повідомлення без запуску основної моделі стилів.

`/api/analyze/gemini` формує розширений опис через Gemini API. Цей маршрут доступний для авторизованих користувачів.

### Дані користувача

```text
GET /api/user/history
GET /api/user/stats
GET /api/user/preferences
```

Ці маршрути доступні лише після входу в систему.

### Робота з моделями

```text
GET  /api/models/available
GET  /api/models/current
POST /api/models/switch
GET  /api/models/info/<model_type>
```

### Зображення датасету для фону

```text
GET /api/dataset/images
GET /api/dataset/image/<filepath>
```

## Змінні середовища

Перед запуском потрібно створити файл `.env` у корені проєкту.

Приклад:

```env
GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_KEY_1=
GEMINI_API_KEY_2=
GEMINI_API_KEY_3=
SECRET_KEY=
```

`SECRET_KEY` потрібен для роботи Flask-сесій.

Ключі Gemini API потрібні для формування розширеного ШІ-опису. Якщо ключі не задані або ліміт Gemini вичерпано, базова класифікація зображень власними моделями все одно може працювати.

## Файли моделей

Для роботи класифікації потрібні навчені checkpoint-файли моделей. Вони очікуються у директорії:

```text
checkpoints/
```

Очікувані файли:

```text
checkpoints/best_model_arch_or_not.pth
checkpoints/efficientnet_b0_best.pth
checkpoints/resnet50_best.pth
```

Якщо checkpoint-файли основних моделей відсутні, вебзастосунок може запуститися, але класифікація відповідними моделями буде недоступною.

Якщо файл `checkpoints/best_model_arch_or_not.pth` відсутній, попередня перевірка `architecture / not_architecture` пропускається, а система працює за старим сценарієм основної класифікації.

## Локальний запуск

### 1. Клонувати репозиторій

```bash
git clone https://github.com/yana-donda/ArchVisionAI.git
cd ArchVisionAI
```

### 2. Створити віртуальне середовище

```bash
python -m venv .venv
```

Активувати середовище:

```bash
# Windows
.venv\Scripts\activate
```

```bash
# Linux / macOS
source .venv/bin/activate
```

### 3. Встановити залежності

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Створити `.env`

```bash
cp .env.example .env
```

Після цього потрібно заповнити `SECRET_KEY` і, за потреби, Gemini API keys.

### 5. Підготувати датасет

Для навчання моделей потрібно завантажити датасет **Architectural styles** з Kaggle:

```text
https://www.kaggle.com/datasets/dumitrux/architectural-styles-dataset
```

Після завантаження дані потрібно розмістити у форматі `ImageFolder`, тобто кожен архітектурний стиль має бути окремою папкою з відповідними зображеннями.

Очікувана структура датасету:

```text
dataset/
├── train/
│   ├── Achaemenid architecture/
│   ├── American Foursquare architecture/
│   ├── American craftsman style/
│   └── ...
├── val/
│   ├── Achaemenid architecture/
│   ├── American Foursquare architecture/
│   ├── American craftsman style/
│   └── ...
└── test/
    ├── Achaemenid architecture/
    ├── American Foursquare architecture/
    ├── American craftsman style/
    └── ...
```

Папка `train` використовується для навчання моделей, `val` – для перевірки якості під час навчання та вибору найкращих ваг, а `test` – для фінального оцінювання моделі після завершення навчання.

Назви папок класів у `train`, `val` і `test` мають збігатися, щоб модель працювала з однаковим набором архітектурних стилів на всіх етапах.

### 6. Навчити моделі та отримати checkpoint-файли

У проєкті передбачені окремі скрипти для навчання моделей **EfficientNet-B0** і **ResNet-50**.

Для навчання EfficientNet-B0 потрібно виконати:

```bash
python train_efficientnet_b0.py
```

Для навчання ResNet-50 потрібно виконати:

```bash
python train_resnet50.py
```

За замовчуванням ці скрипти використовують такі шляхи до датасету:

```text
dataset/train
dataset/val
```

Після запуску навчання автоматично створюється директорія:

```text
checkpoints/
```

У цій директорії зберігаються файли з найкращими вагами моделей:

```text
checkpoints/efficientnet_b0_best.pth
checkpoints/resnet50_best.pth
```

Саме ці checkpoint-файли використовуються вебзастосунком для класифікації архітектурних стилів.

Також під час навчання створюється директорія `training_runs`, у якій зберігаються логи, метрики та короткі підсумки навчання.

Якщо потрібно явно вказати шляхи до датасету або змінити кількість епох, скрипти можна запускати з параметрами:

```bash
python train_efficientnet_b0.py --data-dir dataset/train --val-dir dataset/val --epochs 20
```

```bash
python train_resnet50.py --data-dir dataset/train --val-dir dataset/val --epochs 20
```

За потреби можна додати параметр `--pretrained`, щоб використати попередньо навчені ImageNet-ваги:

```bash
python train_efficientnet_b0.py --pretrained
```

```bash
python train_resnet50.py --pretrained
```

Якщо checkpoint-файли вже були отримані раніше, повторно навчати моделі не потрібно. Достатньо просто помістити файли `efficientnet_b0_best.pth` і `resnet50_best.pth` у директорію `checkpoints`.

### 7. Навчити бінарний фільтр architecture / not_architecture

Для перевірки того, чи містить вхідне зображення архітектурний об’єкт, використовується окрема модель EfficientNet-B0 з двома класами.

Для її навчання потрібно підготувати датасет у такому форматі:

```text
dataset/
├── train/
│   ├── architecture/
│   └── not_architecture/
└── val/
    ├── architecture/
    └── not_architecture/
```

Клас `architecture` формується із зображень будівель і фасадів, а клас `not_architecture` – із зображень інших об’єктів.
```

Навчання бінарного фільтра виконується командою:

```bash
python train_arch_or_not.py --epochs 5 --batch-size 32 --lr 0.0001
```

Скрипт використовує попередньо навчені ImageNet-ваги EfficientNet-B0 і донавчає модель на двох класах.

Після навчання найкращі ваги зберігаються у файлі:

```text
checkpoints/best_model_arch_or_not.pth
```

Також створюється metadata-файл:

```text
checkpoints/best_model_arch_or_not.json
```

### 8. Запустити застосунок

Після встановлення залежностей, налаштування `.env` і додавання checkpoint-файлів застосунок можна запустити командою:

```bash
python app.py
```

Локально вебзастосунок буде доступний за адресою:

```text
http://localhost:8002
```

## Запуск через Docker

### 1. Зібрати Docker-образ

```bash
docker build -t archvisionai .
```

### 2. Підготувати файл бази даних і директорію моделей

```bash
mkdir -p checkpoints
touch archvision.db
```

### 3. Запустити контейнер

```bash
docker run -d \
  --name archvisionai \
  -p 80:8000 \
  --env-file .env \
  -v $(pwd)/checkpoints:/app/checkpoints \
  -v $(pwd)/archvision.db:/app/archvision.db \
  archvisionai
```

Після запуску застосунок буде доступний за адресою:

```text
http://localhost
```

Для Windows PowerShell шляхи до volume можуть потребувати адаптації.

## Тестування моделей

Під час тестування було перевірено такі режими:

- EfficientNet-B0;
- ResNet-50;
- Ensemble;
- EfficientNet-B0 + TTA;
- ResNet-50 + TTA;
- Ensemble + TTA.

Узагальнені результати:

| Модель | Точність | F1-macro | Час аналізу |
|---|---:|---:|---:|
| EfficientNet-B0 | 86.13% | 0.8583 | 0.0442 с |
| ResNet-50 | 87.21% | 0.8709 | 0.0476 с |
| Ensemble | 88.44% | 0.8824 | 0.0942 с |
| EfficientNet-B0 + TTA | 87.48% | 0.8718 | 0.303 с |
| ResNet-50 + TTA | 88.05% | 0.8796 | 0.312 с |
| Ensemble + TTA | 89.73% | 0.8952 | 0.6451 с |

Найкращу якість класифікації показав режим **Ensemble + TTA**.

## Автоматизоване тестування застосунку

У проєкті реалізовано автоматизовані тести для перевірки серверної частини, API-маршрутів, інтеграції моделей комп’ютерного зору, Gemini API та розгорнутого вебзастосунку.

### Встановлення залежностей

Перед запуском тестів потрібно встановити залежності з файлу `requirements.txt`:

```bash
pip install -r requirements.txt
```

### Локальні інтеграційні тести

Локальні інтеграційні тести перевіряють роботу Flask-застосунку, автентифікації, захищених маршрутів, API моделей, аналізу зображень, збереження історії, статистики та архітектурних вподобань користувача.

Запуск локальних тестів:

```bash
python -m pytest -q tests/test_app.py
```

Ці тести використовують реальні компоненти застосунку: `AnalysisService`, `ArchVisionAnalyzer`, SQLite-базу даних і checkpoint-файли моделей. Для їх виконання потрібні такі файли:

```text
data/class_mapping.json
checkpoints/best_model_arch_or_not.pth
checkpoints/efficientnet_b0_best.pth
checkpoints/resnet50_best.pth
```

Окремо можна запустити тест інтеграції з Gemini API. Для цього потрібно задати ключ Gemini у змінній середовища:

```powershell
$env:GEMINI_API_KEY_1="your_gemini_api_key"
python -m pytest -q tests/test_app.py -k gemini
```

Якщо Gemini повертає непорожню відповідь, тест вважається успішним.

### Тести розгорнутого застосунку

Окремо реалізовано тести для перевірки розгорнутої версії застосунку через HTTP-запити. Вони перевіряють доступність головної сторінки, статичних ресурсів, повторювані запити, автентифікацію, аналіз зображень у різних режимах, історію користувача та одночасну роботу кількох користувачів.

Перед запуском потрібно вказати адресу розгорнутого застосунку. Також у локальній папці `dataset/` має бути хоча б одне реальне зображення будівлі або фасаду, яке буде використане як тестове зображення для HTTP-запитів до розгорнутого застосунку:

```powershell
$env:ARCHVISION_BASE_URL="http://archvisionai.swedencentral.cloudapp.azure.com/"
```

Запуск deployment/load тестів без Gemini:

```powershell
python -m pytest -q tests/test_deployed_load.py -m "deployed and not gemini" -s
```

Кількість одночасних користувачів можна змінити через змінну середовища:

```powershell
$env:ARCHVISION_LOAD_USERS="5"
python -m pytest -q tests/test_deployed_load.py -m "deployed and not gemini" -s
```

Для запуску Gemini-тесту на розгорнутому застосунку потрібно додатково ввімкнути відповідний прапорець:

```powershell
$env:RUN_DEPLOYED_GEMINI_TEST="1"
python -m pytest -q tests/test_deployed_load.py -m gemini -s
```

Після запуску deployment/load тестів створюється файл:

```text
test_results/deployed_load_results.csv
```

У ньому зберігаються сценарій тесту, HTTP-статус, час відповіді, використаний режим моделі та результат виконання.

## Безпека

У застосунку реалізовано базові механізми безпеки:

- паролі не зберігаються у відкритому вигляді;
- для паролів використовується PBKDF2-HMAC-SHA256;
- для кожного користувача генерується окреме salt-значення;
- обмежено максимальний розмір вхідного файла;
- секретні ключі мають зберігатися у `.env`;
- `.env` не слід додавати до GitHub-репозиторію.

## Обмеження

Поточна версія має кілька практичних обмежень:

- якість результату залежить від ракурсу, освітлення та якості зображення;
- візуально схожі архітектурні стилі можуть плутатися між собою;
- бінарний фільтр `architecture / not_architecture` залежить від якості та різноманіття навчальних даних і може помилятися на складних або неоднозначних зображеннях;
- Gemini API має власні ліміти запитів;
- SQLite підходить для демонстраційного або невеликого застосунку, але для більшого навантаження краще перейти на серверну СКБД.

## Можливі напрями розвитку

У майбутньому проєкт можна розширити такими покращеннями:

- додати більше архітектурних стилів;
- збалансувати навчальний датасет;
- додати більше зображень у різних умовах освітлення, масштабу та ракурсу;
- покращити класифікацію візуально схожих стилів;
- показувати користувачу кілька найімовірніших стилів з поясненням;
- додати сегментацію архітектурних елементів;
- інтегрувати онлайн-каталоги архітектурних пам’яток;
- розширити базу географічних регіонів і відомих будівель;
- покращити персоналізовані рекомендації на основі історії аналізів;
- реалізувати англомовний інтерфейс або повну багатомовну підтримку для користувачів з інших країн;
- перейти на PostgreSQL або іншу серверну базу даних для production-сценаріїв.

## Автор

Проєкт виконала Донда Яна, студентка групи ПМІ-43 факультету прикладної математики та інформатики Львівського національного університету імені Івана Франка. Застосунок створено в межах виконання кваліфікаційної (бакалаврської) роботи зі спеціальності 122 – «Комп’ютерні науки» на тему:**«Розробка інтелектуальної вебсистеми аналізу та класифікації архітектурних стилів на основі технологій комп’ютерного зору»**.
